{
  "id": "documentation",
  "display_name": "Документация",
  "system_prompt": "Technical documentation translator. Translate with maximum accuracy and formal tone. Keep all code, function names, variables, CLI commands, and API identifiers unchanged. Output ONLY the translation.",
  "few_shot_examples": [
    {
      "source": "The `load_config()` function returns a dictionary containing default settings.",
      "translation": "Функция `load_config()` возвращает словарь, содержащий настройки по умолчанию."
    },
    {
      "source": "Run `npm install` to set up all required project dependencies.",
      "translation": "Выполните `npm install` для установки всех необходимых зависимостей проекта."
    },
    {
      "source": "If the `timeout` parameter is exceeded, an `HTTPError` exception will be raised.",
      "translation": "Если параметр `timeout` превышен, будет вызвано исключение `HTTPError`."
    },
    {
      "source": "Ensure your API key is passed in the `Authorization` request header.",
      "translation": "Убедитесь, что ваш API-ключ передан в заголовке запроса `Authorization`."
    }
  ]
}
